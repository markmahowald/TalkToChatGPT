<Window x:Class="ChatWithChatGpt.NewConversationMessageBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatWithChatGpt"
             mc:Ignorable="d" 
             Height="150"
        Width="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Content="Who is GPT in this conversation?" HorizontalAlignment="Center" Margin="10" />

        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="10" />

        <Button x:Name="StartChattingButton" Content="Start Chatting" Grid.Row="2" HorizontalAlignment="Center" Margin="10"
                Click="StartChattingButton_Click"/>
    </Grid>
</Window>
